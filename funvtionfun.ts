//creating a function with three parameters.
//assigning types to all input parameters and final out value.

const fun = (a:number, b:number, c:number):number => {
return a+b+c;
}

console.log(fun(1,2,3));

