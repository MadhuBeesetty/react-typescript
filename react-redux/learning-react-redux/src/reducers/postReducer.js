import { FETCH_POSTS, NEW_POSTS } from '../actions/types';

const initialState = {
  iteams: [],
  item: {}
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
    return state;
  }
}